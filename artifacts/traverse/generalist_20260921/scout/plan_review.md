# Plan review: merged findings (3 reviewers, 44 findings -> 38 after merge)

Merge editor, 2026-09-21. Every cited code line, data count and memory note was re-read this session; numbers marked 'verified' were recomputed. Plan under review: `PLAN.md`; handoff: `PLAN_handoff.md`.

Dropped / corrected: the ML reviewer's '55 x 12 = 660 designed test routes' is wrong (twin_crm test split = 422 designed + 271 on-policy rows; only 15,235 of the 24,000 tasks were driven in CRM); the 'undefined 200 routes' point survives inside finding 4. Merged: B5 harvesting leak (stats + ML) -> 1; A5 specialist-training confound (stats + ML) -> 9; branch labeller (sim + ML) -> 16; A1b probe (stats + ML) -> 28; CRM own-remainder duplicate (ML) folded into 13; critic item 6 (margin) into 7; hist_rma downgraded to minor (wasted arm, not a corrupted result); tracker brake squash downgraded to major (config-line fix, must be in the plan).

| rank | severity | section | claim | fix |
|---|---|---|---|---|
| 1 | blocking | B5 improvement round (PLAN.md:140-141), B1 held-out (PLAN.md:111-112), B4 gate (PLAN.md:132-133,160), tracker env split | The B0 test routes leak into model fitting through three paths: the improvement round harvests policy failures from the only policy drives scheduled (the B0 suite) and refits the NRD on them; the forked tracker env draws PPO fragments with the fixed-seed random episode split, not the group split, so held-out-group recordings and their route previews seed imagination; and the B4 NRD gate and variant selection run on the 111-group held-out set that contains the B0 routes. | Freeze the B0 suite and its PID drives before round 1. Harvest policy failures only on training-group routes (e.g. 1,000 routes from the 1,089 train groups in both worlds, ~3-4 node-hours added to the B budget). Make gb_tracker_env.py and gb_build_cache.py take the group split from the cache manifest, train on train groups only, and assert that no B0 route id or held-out group id is in the fragment bank or the NRD train split. Run the B4 gate and NRD checkpoint selection on the 56 val groups; reserve the 55 test groups for B0 and the final offline report. |
| 2 | blocking | Conventions (PLAN.md:30 'mandatory --crm-config'), cluster roots (PLAN.md:6), A4/A5/B3/B5 CRM drives | Any CRM drive through the existing worker with a collector named gc_collect_branch.py or gc_collect_ext.py silently runs at the in-script default 0.5 ms step instead of the 1 ms crm_main.json config. | Make the gc_ scripts refuse to start without --crm-config (assert), and either name them with the crm_collect substring (crm_collect_branch.py, crm_collect_ext.py) or put ['--crm-config', '<absolute path>'] in each task row's extra; read physics_dt_s from the first smoke's outcome.json and require 0.001 before launching. |
| 3 | blocking | Cluster roots (PLAN.md:7-8 'rigid drives via the proven gen_array.sbatch'), A4 (PLAN.md:84-87), B3 (PLAN.md:120-126), B5 (PLAN.md:140) | The rigid world has no launch path for any new collector mode: gen_array.sbatch/gen_runner.py can only run gen_v1/gen_collect.py, and the CRM-worker alternative under CRM_ROOT=C fails the manifest and fingerprint gates. | Ship G/source with a source_manifest.json (recipe in scout/cluster_data_inventory.md section 5a); copy gen_array.sbatch + gen_runner.py into G with GEN_ROOT=$G and a GEN_COLLECTOR env var (default gen_collect.py); amend the plan's root convention so rigid new-mode drives run from G and only the unmodified A0/A3 drives stay on R; run one `gen_collect.py --check-only` against G/source before any rigid launch. |
| 4 | blocking | Milestone B primary statistic and suite (PLAN.md:31-34, 36-39, 102-106) | The primary tracking statistic (time-domain cross-track RMSE 'up to the failure', mean over routes, failed runs kept) rewards early stopping and is tail-dominated, and on CRM the reference PID completes only about a third of the designed test routes, so the suite is mostly a failure contest; '200 routes' is also undefined (the 55 test groups hold 422 designed routes in the CRM twin set, 271 on-policy). | Predeclare a station-based, completion-aware primary: for every reference station the distance to the nearest trajectory point, unreached stations capped at the 6 m off-route abort; report paired median and Winsorised mean. Use all 422 designed test routes in both worlds (or a seeded predeclared subset), stratified in advance by 'recorded PID reached the goal in both worlds' (~130 routes): cross-track is the primary on the feasible stratum, completion on the infeasible stratum. Write the selection rule into B0. |
| 5 | blocking | Milestone B thresholds (PLAN.md:37-39) | All four 'no material regression' rules are point-estimate rules with no noise allowance, so a policy exactly equal to PID fails the milestone most of the time, and the 10 % primary is powered only for a 30-50 % effect with failed runs kept. | Replace point rules with one-sided 95 % bounds and margins: lower bound of the paired completion difference > -3 points; upper bound of the paired 'any unsafe event' rate difference <= +1 point (one pooled safety event, not three counts); upper bound of the speed-error ratio < 1.10. For the primary state the rule exactly ('upper 95 % paired-bootstrap bound of the ratio < 0.90' or 'point <= 0.90 and CI excludes 0'; they differ) and use the feasible stratum with the station metric so cv ~0.5 makes a 20 % effect detectable at n ~130-200. |
| 6 | major | B5 tracker action squash (PLAN.md:136-142) vs handoff 'matched actuator limits' (PLAN_handoff.md:46,53) | A tracker forked from tracker_env.py with its default action centre/scale can never command brake above ~0.52 (or throttle above ~0.9), so the PID-vs-policy comparison is not at matched actuator limits and the policy is handicapped on the descents and parking stops where the f104 PID brakes hard; the imitation targets for those frames are also silently truncated. | In gb_tracker_env.py set brake to center 0.5 / scale 0.5 and throttle to center 0.5 / scale 0.5 so the squash spans the full [0,1] box; write centre/scale into policy_meta.json and assert in gb_track_analyze.py that PID and policy arms share action bounds. |
| 7 | major | Milestone A statistic (PLAN.md:35-37, 97-98) and A3/A5 arm lists (PLAN.md:76-78, 93) | The 600-group suite with a 2-point margin is at the edge of feasibility (a truly equivalent shared model passes only ~50-80 % of the time per domain, ~0.6 jointly), the decision statistic is ambiguous (two-sided 97.5th vs one-sided 95th percentile; primary arm and condition unnamed), and the 9-cluster CI cannot serve as the 'honest' interval. | Write the decision rule: primary arm H (hist_aux) at startup in A3, one-sided 95th percentile of the group-bootstrap distribution of (specialist - shared) goal-reached < margin; widen the margin to 3 points or raise the suite to ~1,000 groups (simulated pass at equivalence 0.94 at 3 % discordance); declare A5 established-history H a secondary with its own bound; declare the 9-cluster interval a robustness report, not the decision statistic. |
| 8 | major | Milestone A on rigid ground and cross-specialist controls (PLAN.md:35-37, 43-49, 97-98) | Goal-reached is at the ceiling for the rigid specialist under the speed-free regime, so the rigid half of Milestone A cannot detect the expected failure mode of a shared model (over-conservatism), and the S_crm-on-rigid control will look like a success. | Predeclare a rigid secondary with headroom: paired median commanded time (or elapsed) of H not more than 10 % above S_rigid with a one-sided bootstrap bound, and a pick-agreement statistic (H agrees with S_own more often than with S_other, per domain). Report S_crm-on-rigid and S_rigid-on-CRM as the gap to close in both goal-reached and time; if the rigid time gap is < 2x the margin, state that rigid does not test adaptation. |
| 9 | major | A5 established-history closed loop and A4 retraining (PLAN.md:43-49, 87-88, 90-95, 158-159) | The established-history comparison is confounded: the specialist reference arms (CRM_N2, N2) were trained on standing-start rows only while H is retrained on reanchor + branch rows with moving anchors; the 'history masked' ablation is out-of-distribution at a moving anchor (masked == at-rest in training); and T, the declared ceiling, is not retrained on branch rows. | In A5 drive the ga_train --domain-filter ensembles (geometry ctx, deploy mode, reanchor + branch rows) as the specialist arms and keep CRM_N2/N2 only for A0/A3 (and as a secondary); retrain T and P on the same rows; add random window-drop augmentation so the masked-history ablation is in-distribution, or use P as the no-history control. |
| 10 | major | A5 common approach (PLAN.md:90-92) | The straight 3 s approach stalls a large share of the CRM suite before any arm gets to plan, and the '12 m of route_00' fallback is rejected by both collectors. | Drive pass 1 with a full route (straight start-goal line or route_00) and --horizon-s 3; predeclare the established-history analysis set as groups whose frame-60 state is moving in both worlds (vx > 1 m/s, sinkage increase < 0.1 m) and report the excluded count; optionally choose the approach heading as the lower-grade of the straight line and route_00's first tangent. |
| 11 | major | A4 anchor sampling and offline established read-out (PLAN.md:56-57, 84-85, 97) | '800 anchors from the 1,200 training groups' is wrong or leaky: only 1,089 groups are training groups; taken literally it puts branch rows of held-out groups into training, and if restricted to train groups there is no held-out branch row, so the offline 'established' metrics stay on the survivorship-biased re-anchored rows. | Sample anchors per split (e.g. 700 from the 1,089 train groups, 100 from the 111 held-out groups), keep the group split for branch rows, extend the blacklist assertion to group ids, and report offline established metrics on the held-out branch rows. |
| 12 | major | Held-out set reuse for selection (PLAN.md:56-57, 111-112, 128-134, 160) | Val and test groups are merged into one 111-group held-out set used both for model selection (A2 conditioning choice, B4 gate 'after two model variants', NRD checkpoint selection) and for the final read-outs. | Use the 56 val groups for all gates and variant selection (A2 conditioning choice, B4 NRD gate, checkpoint selection) and reserve the 55 test groups for the final offline report and the B0 suite only. |
| 13 | major | A4 'CRM prefixes reproduce bit-identically' (PLAN.md:82,84), own-remainder continuation (PLAN.md:86), stop rule (PLAN.md:161) | Bit-identical prefix reproduction is asserted for a collector that does not exist yet and the only planned check (local byte-for-byte smoke) cannot establish it; the own-remainder continuation is either a duplicate of the recording or the determinism check, not new data. | Before the 2,400 drives, re-drive 5-10 recorded collect_v1 episodes on the cluster (mi3501x, ~0.1 billed hour) through the new script in no-branch mode with CRM_OMP=4 and compare trajectory.npz to C/collect_v1/runs byte-for-byte; run two unmodified local episodes first to learn whether the local gate is meaningful and move the gate to the cluster if not. Count the own-remainder drive as this determinism check (or take its row from the recording), not as new data; keep the third drive only in rigid where per-node noise makes the replay non-identical. |
| 14 | major | A4 rigid branches (PLAN.md:84-87: 'all branches of an anchor on one node', continuations 'from the recorded pose at F', 320 low-progress anchors) | Rigid prefix replays are not the recorded episodes: the recording came from a different node, so the pose at F and, for low-progress anchors, the stall itself may not recur. | Use the A5 two-pass protocol for rigid A4: pass 1 re-drives each prefix with --horizon-s F on the assigned node and records pose/history at F; keep only anchors whose replayed state matches the recording's class (stalled vs moving, pose within 0.5 m); plan the 3 continuations from the replayed pose and run pass 2 on the same node; report the drop count. |
| 15 | major | A4 low-progress anchors 'cut 1 s after onset' (PLAN.md:85-86) | One second after stall onset the CRM vehicle is already digging in, so the 960 low-progress CRM continuations have little room to differ and many anchors are past recovery. | Define onset from the recorded fields as above, cut between onset-1 s and onset+0.5 s, and pre-screen anchors locally with crm_extra.npz (spindle_z - bmp_ground_z) to keep only those whose sinkage increase at the cut is < 0.1 m; report how many survive and their onset-to-termination margin before launching. |
| 16 | major | A4 branch labels (PLAN.md:87 'Labels by the night-2 labeller on the branch segment') | The existing labellers read whole-episode outcome fields and trajectories from frame 20, so a stalled prefix contaminates every continuation's label; applied to a branch that starts 1 s after a stall onset they put the event at station ~0 for every continuation. | Have the branch collector write branch_frame and the branch route hash into outcome.json and trajectory.npz; make the ga_ labeller slice at branch_frame, project only onto the branch route, start the event clock after a grace window (first frame >= 1 m from the branch pose, or 3 s), label low-progress branches primarily by goal reached from the branch with rollback as a secondary flag; check offline that continuations of the same anchor have discordant labels before training. |
| 17 | major | B0 (PLAN.md:102-104), B3 stall rule (PLAN.md:123-125), B5 'PID arm in the same job' (PLAN.md:140), thresholds (PLAN.md:36-39) | PID and policy arms are terminated by different rules and the plan does not say which PID is 'the current PID', so completion, time and failure-truncated cross-track are not paired quantities. | Run every B0 arm through gc_collect_ext.py with one identical stop-rule set (low-throttle rule for all arms or none), with two PID arms: native (unmodified collector, the milestone reference) and held (callback = shadow follower output, the actuator-matched control); state in the plan which arm the 10 % threshold is tested against. |
| 18 | major | Conventions (PLAN.md:27-28 'hold one action for the whole 50 ms interval and record exactly that action'), B3 (PLAN.md:121-123 '2/s steering rate') | If the external-control mode keeps the collectors' per-substep steering clamp, the held command is ramped inside the interval and the recorded action[k] is not the applied action. | In gc_collect_ext.py clip the command once per frame (cmd[0] = clip(cmd[0], prev - 0.1, prev + 0.1)), write that exact triple at all substeps, and assert in the smoke test that the substep log's per-interval max-min is 0 for held drives. |
| 19 | major | B3 'policy: torch actor on CPU' (PLAN.md:122), B5 CRM evaluation via gc_collect_ext.py --mode policy (PLAN.md:140) | Importing torch inside the CRM collector process is untested and likely broken on most CRM partitions. | Export the actor (obs-norm + MLP + tanh + action affine) to an npz and implement act() in numpy inside gc_collect_ext.py (traverse_wp3_chrono_eval.py:134-141 is ~10 lines); keep torch out of every collector process; smoke the numpy actor against the torch one offline (max /delta action/ < 1e-5). |
| 20 | major | Order and budget (PLAN.md:146-151 'Wave 1 (parallel)', 'Wave 2') | The parallel waves exceed the 50-queued-jobs cap because each crm_launch.sh call queues 47 array tasks and each rigid array 24. | Merge each wave's CRM tasks into one tasks.json per world (the worker accepts heterogeneous rows with per-task extra; prefix ids by step) and launch once; run rigid arrays with 3 shards on mi2104x (128 cores) instead of 24 on mi2101x; count array tasks before every sbatch. |
| 21 | major | B5 fragments and observation history (PLAN.md:137-139) vs Chrono evaluation from rest | The policy is never trained from a standstill start with a padded history, but every Chrono suite route starts at frame 0 from a braked rest, so the launch phase of every evaluation route (on 0.6-slope terrain) is out of distribution for both the policy and the NRD context. | In gb_tracker_env.py allow start_frames in [0, active_end - len] and pad NRD context, action history and state history for start < 16 with the frame-0 rest state and the settle action (0,0,1) (the build_history convention, fdm_data.py:79-91); mirror the same padding in gc_collect_ext.py --mode policy. |
| 22 | major | B4 gate and hold_ok targets (PLAN.md:128-134) with the drift filter (PLAN.md:27-29) | The drift filter removes every recorded brake-onset transition (and 5-10 % of fast-moving ones) while keeping 98-99 % of stalled ones, so the B4 validation on hold_ok-filtered targets never tests the model's throttle-to-brake response. | Report retention per regime (stalled / moving / brake-onset) in gb_build_cache.py; add to the B4 validation a held-out slice of B3 hold-mode episodes (true zero-order hold, including brake taps) and require the vx-response error on brake-onset windows to meet the same 25 % bound; size B3 so at least ~2,000 brake onsets per world exist. |
| 23 | major | B1 cache schema (PLAN.md:108-112) and B4 training on mi3501x (PLAN.md:133-134) | Storing per-frame terrain8 and terrain16 crops in a padded schema-v2 cache for 39k episodes needs ~50-100 GB of host RAM and is useless for the rollout paths, which must re-crop at predicted poses anyway. | Do not store crops. Implement one ego_elevation_crop(grid, pose, k, half_m) module (grid_sample on the single 512x512 v2 grid on device, terrain_crop_maps.md section 3) and use it in the step loss, rollout loss, evaluation and imagination; cap padding with --n-frames or store ragged episodes; the cache is then z1/act/pose/power/domain only (< 6 GB). |
| 24 | major | B5 PID-imitation warm start (PLAN.md:139) with rsl_rl empirical observation normalisation | A supervised warm start of the ActorCritic is undone at the first PPO iterations because OnPolicyRunner's EmpiricalNormalization keeps refitting the observation scaling for the first 1e8 samples (~760 of 1,000 iterations), and the plan does not say how the imitation stage's inputs are scaled. | In gb_train_tracker.py fit the runner's obs_normalizer on the imitation observation set before PPO (or load its state_dict), train the imitation head on the normalised obs, and log actor-vs-PID action MSE at PPO iteration 0 and 10. |
| 25 | major | A2 calibration metrics (PLAN.md:69) and Milestone A read-out (PLAN.md:97-98) | The survival model estimates P(unsafe), not P(fail); in the rigid world these differ by 17 points, so Brier/ECE against the goal-reached label would be miscalibrated by construction and the plan does not say which label is used. | Declare in ga_train.py that calibration (Brier, ECE) is computed against unsafe (the fitted event) and report fail-calibration only as a secondary row with the unsafe-not-fail rate alongside; if goal-reached calibration is wanted, add a separate fail head or a monotone recalibration fitted on the dev fold. |
| 26 | minor | A0 suite composition (PLAN.md:43-49) | Reusing the 200 night-2 CRM eval groups with their existing CEM 4x64 drives as the specialist reference imports a winner's curse: CEM 4x64 was chosen on those groups among near-tied arms. | Report the 400 fresh f104_pair groups as the primary stratum and the 200 reused groups separately, or drop the reused groups from the decision statistic. |
| 27 | minor | Milestone A read-out on suite drives (PLAN.md:97-98) | Route-ranking AUC and calibration computed on the suite drives are computed on each arm's argmin picks, a low-risk tail sample, not on a representative set of routes. | Make the offline held-out rows (twin + held-out branch rows) the primary ranking/calibration read-out and label the suite-drive numbers as 'on selected picks'. |
| 28 | minor | A1b identifiability probe (PLAN.md:59-62) | The probe is mis-specified at both ends: on established rows domain AUC is confounded by world-specific anchor selection (different anchor sets and per-world speed stratification), and on startup rows a GRU on the all-masked window is AUC 0.5 by construction, not ~0.55. | Run the established probe on the 44,990 coinciding (episode, frame) pairs and on the A4 branch rows where the prefix is identical by construction; run the startup probe on the frame-0 12-column state (per-column and joint logistic AUC) and state the all-masked result as 0.5 by definition. |
| 29 | minor | A2 hist_rma arm (PLAN.md:66-67) vs handoff step 2 (PLAN_handoff.md:26) | With a two-class privileged variable the teacher latent carries one bit plus geometry the history encoder cannot reproduce, so hist_rma is a relabelled hist_aux with a noisier target; it does not implement the handoff's 'small task-relevant latent learned by the privileged teacher' and spends 2 x 5 seeds x 2 stages for no new information. | Drop hist_rma, or make the teacher genuinely privileged: give it simulator-only continuous signals (tyre Fz cols 7-10, motorshaft torque col 16, CRM slip/sinkage from crm_extra.npz) as a 4-8 d privileged context, train tag+priv, then regress z onto that embedding; report both. |
| 30 | minor | A1 mixed dataset construction (PLAN.md:51-55) | Concatenating twin and reanchor files duplicates every startup row: the reanchor k = 0 rows are identical to the twin rows. | In ga_build_mixed.py drop reanchor rows with anchor_frame == 0 (or drop the twin file) and assert uniqueness of (episode, anchor_frame, domain). |
| 31 | minor | Budget figures (PLAN.md:49, 87, 94, 126, 152-153) and B4 on mi3501x (PLAN.md:133-134) | The node-hour figures mix billed units with wall-hours and omit the 4 h wall-clock cap that binds on the single-GPU partitions. | Restate each step as (sim-hours, GPU-hours, billed) with the rate table and plan CRM launches around 4 h windows (worker resume = resubmit) or the mi2508x/mi3508x nodes. |
| 32 | minor | B4 'resume from checkpoint if needed' (PLAN.md:133-134); B5 PPO under the same cap | Neither trainer being forked can resume: the NRD trainer restores weights only and the PPO runner has resume hard-coded off. | In the forks save optimizer/scheduler/step in ckpt_last.pt and add --resume; set rsl_rl's resume/resume_path from an argument; otherwise size runs to < 3.5 h and say so. |
| 33 | minor | A4 driver swap (PLAN.md:83 'make_driver(branch), Reset(), no Initialize(), steering state kept') | The named helper always calls Initialize(), so the plan's line contradicts itself; Reset() is already done by the constructor. | In gc_collect_branch.py rebuild the 8 lines of make_driver without Initialize() (points, Bezier, gains, look-ahead); document that Reset() happens in the constructor and that the first branch substep passes through the existing +/- 2*dt clamp to keep steering continuous. |
| 34 | minor | Wave 1 local work (PLAN.md:146-147) and B2 (PLAN.md:114-118) | Local feasibility is asserted without the two numbers that decide it (CRM GPU memory next to torch, CRM/torch contention), and a local rigid smoke cannot exercise the cluster's manifest/fingerprint gates. | Serialise local GPU work (CRM smoke/audit first, A2 after), record nvidia-smi peak memory in the first CRM smoke's report, and run one gen_collect.py --check-only against the shipped G/source before any rigid launch. |
| 35 | minor | B1 cache fields and B4 gate on 'held-out stalled windows' (PLAN.md:110, 132-133) | The cache as specified carries only an episode-level status, so the gate's stalled-window population is not computable from it as written. | Add a per-frame stalled (T,) bool and the blockage window starts to the cache in gb_build_cache.py with the rule in the manifest; define a gate window as 60 consecutive stalled frames in a held-out episode. |
| 36 | minor | A3 startup closed-loop arms (PLAN.md:76-78) | At startup the H arm's history is all-masked, so driving P as well as H buys little beyond what the offline startup metrics already give, at ~1,200 extra drives. | Drive T and H at startup; report P offline only, or drive P on a 200-group subset as a sanity check. |
| 37 | minor | B5 choice of frozen NRD (PLAN.md:129, 136) | B4 trains tag and notag NRD arms but B5 does not say which one PPO rolls in, nor how a tag NRD gets its domain per env. | State that PPO uses the tag NRD (the env knows each fragment's domain) unless the notag arm passes the B4 gate equally, and record the NRD checkpoint hash in policy_meta.json. |
| 38 | minor | A2 compute estimate (PLAN.md:70) | The quoted 12 s / 2-3 min per seed are single-world numbers; the mixed twin+reanchor set with a 40-step history GRU is ~2.5x the rows and the full arm matrix is several hours on the 5090, not a quick wave-1 item. | Budget ~6-8 h on the 5090 for A2 (or run holdout locally and deploy ensembles on mi3501x with the n2_train.sbatch shape) and drop hist_rma unless made privileged. |

## Evidence and consequence per finding

**1. [blocking] B5 improvement round (PLAN.md:140-141), B1 held-out (PLAN.md:111-112), B4 gate (PLAN.md:132-133,160), tracker env split**

- Evidence: PLAN.md:140-141 ('Chrono evaluation ... on the B0 suite ... harvest policy failures, add them to the cache, refit NRD, retrain PPO'; no other policy drives in B3 or the budget); src/nedm/traverse/tracker_env.py:110 uses D.split_keys = seeded permutation over cache keys (src/nedm/traverse/nrd_data.py:24-35), B5 changes only the map bank; PLAN.md:112 'held-out = the 111 twin held-out groups (which contain the B0 routes)', :133 gate on held-out stalled windows, :160 'gate fails after two model variants'; scripts/traverse_wp2_train_map.py:719-733 selects ckpt on the val split.
- Consequence: Round-2 Milestone B numbers are measured on routes whose failure dynamics the NRD was refit on; even round 1 trains the policy on test-route states with probability ~0.7 per fragment and selects the NRD on the test recordings. The paired PID comparison is not held-out and the milestone claim is unverifiable.

**2. [blocking] Conventions (PLAN.md:30 'mandatory --crm-config'), cluster roots (PLAN.md:6), A4/A5/B3/B5 CRM drives**

- Evidence: scripts/crm_worker.py:98 `if config and 'crm_collect' in collector: cmd += ['--crm-config', ...]` forwards the flag only when the collector path contains the substring 'crm_collect'; PLAN.md:14 names the new collectors gc_*. scripts/crm_collect.py:38 CRM_DEFAULT step_s 5e-4. crm_f104_v1/LOG.md:13: 0.5 ms vs 1 ms agree on 96.5 % of outcomes and the 0.5 ms step runs at half the real-time factor. crm_worker.py:94 passes `extra` verbatim, so route paths in extra must be absolute.
- Consequence: Every A4 CRM branch prefix is driven on different physics from the recorded episode (bit-identity impossible), A5/B3/B5 CRM data mix physics with the specialists' training data, and node-hour estimates double. Nothing errors, so it is noticed only after the drives.

**3. [blocking] Cluster roots (PLAN.md:7-8 'rigid drives via the proven gen_array.sbatch'), A4 (PLAN.md:84-87), B3 (PLAN.md:120-126), B5 (PLAN.md:140)**

- Evidence: scripts/gen_array.sbatch:13 GEN_ROOT=$campaign/gen_v1 and scripts/gen_runner.py:25 `C + '/gen_collect.py'` are hard-coded with no collector variable. Via the CRM worker, crm_worker.py:92 passes --source-root C/source; C/source/source_manifest.json does not exist (verified on the cluster this session), which gen_collect.py:237-242 requires; scripts/crm_collect.sbatch:9 points CHRONO_BUILD at the FSI build and never sets FDM_RUNTIME_FINGERPRINT (gen_collect.py:271-272).
- Consequence: A4 rigid branches (2,400 drives), B3 rigid perturbed collection (1,500) and every B0/B5 rigid policy evaluation cannot be submitted as written; the rigid half of Milestone B has no data or evaluation path.

**4. [blocking] Milestone B primary statistic and suite (PLAN.md:31-34, 36-39, 102-106)**

- Evidence: Verified this session: twin_crm.npz test split = 55 groups, 422 designed + 271 on-policy rows, fail 0.736. Stats reviewer's recomputation from local recordings: CRM PID goal_reached 33.6 % on the 422 designed routes; failed runs score time-RMSE 0.67 m mean / 0.46 m median vs completed 0.54 / 0.50 because the metric stops at the failure; rigid failed-run mean 1.69 m sd 1.59 (cv 1.56). A station-based cross-track separates them (failed 10.5 m vs completed 0.69 m on CRM). PLAN.md:124-125 adds a 4 s low-throttle stall rule for policy drives only, which shortens failed policy runs further. No selection rule for the 200 is given.
- Consequence: A policy that stalls early at low cross-track outscores a PID that drives further and fails later; a policy completing a route the PID fails is penalised for the harder later segment. The mean is driven by a few multi-metre tails, so the 10 % criterion and its bootstrap CI depend on which 2-3 routes go wrong. Milestone B can pass or fail for reasons unrelated to tracking.

**5. [blocking] Milestone B thresholds (PLAN.md:37-39)**

- Evidence: Stats reviewer's simulation at the observed rates: 'completion not lower by more than 1 point' flags an EQUAL policy 19-34 % of the time at n = 200 (1 point = 2 routes); 'stall/rollover/soil-breakthrough counts not higher' flags an equal policy 37-46 % per count (three counts; CRM PID has ~110 breakthroughs per 200 designed routes); joint pass under exact equality ~4 %. Primary power with failed runs kept (cv 1.4-1.6; paired sd equals between-route sd, wp3_chrono_eval_v1/rows.jsonl: 0.042 vs 0.043 at n = 31): 0.00 / 0.01 / 0.05-0.26 at a true 10 / 30 / 50 % improvement; on completed CRM routes only (cv 0.54) 0.15 at 10 %, 0.96 at 20 %.
- Consequence: A genuinely better tracker is reported as failing a regression check by chance, while the primary cannot detect the predeclared 10 %: a coin flip dressed as a predeclared test.

**6. [major] B5 tracker action squash (PLAN.md:136-142) vs handoff 'matched actuator limits' (PLAN_handoff.md:46,53)**

- Evidence: src/nedm/traverse/tracker_env.py:65,341-343: driver = clamp(center + scale*tanh(a)) with action_scale [1.0, 0.7, 0.5] and center = dataset action mean (brake mean ~0.003-0.03); physical_to_policy (:345-348) clamps u to 0.999. Verified this session on 400 production_v3 episodes: brake > 0.52 on 3.7 % of frames, brake == 1.0 on 3.4 %. scripts/traverse_wp3_chrono_eval.py:138 applies the same squash in Chrono.
- Consequence: Cross-track and speed-error deltas, rollover/overspeed counts and the warm start are biased against the policy on exactly the hard segments; the result cannot be called a PID replacement under the handoff's own terms. Downgraded from blocking because the fix is a config line, but it must be written into the plan.

**7. [major] Milestone A statistic (PLAN.md:35-37, 97-98) and A3/A5 arm lists (PLAN.md:76-78, 93)**

- Evidence: eval_iter_crm/analysis.txt: discordant groups between near-equivalent arms E:B 6/200 (6 vs 0), F:B 12/200 (10 vs 2); the stats reviewer also reports B vs C 6/200. Stats reviewer's group-bootstrap simulation: n = 600, true difference 0, pass 0.77 (97.5th) / 0.87 (95th) at 3 % discordance, 0.49 / 0.61 at 6 %; both domains must pass. scripts/n2_planner_analyze.py:53 returns the two-sided [2.5, 97.5] percentiles; the plan says 'upper 95 % bootstrap bound'. A3 lists T, H, P; A5 lists H-measured, H-masked, T; no primary declared. scripts/n2_cluster_ci.py:22 resamples 9 clusters. critic.md item 6 already flagged the margin.
- Consequence: A correct shared model has a large chance of being reported as failing Milestone A; an undeclared primary invites choosing the best of {H-startup, H-established, hist_aux, hist_rma} after the fact.

**8. [major] Milestone A on rigid ground and cross-specialist controls (PLAN.md:35-37, 43-49, 97-98)**

- Evidence: Verified: fdm_f104_50h_20260909/gen_v1/test_results.json rates/f104/n2 fail 0.0, unsafe 0.0, median time 12.55 s vs rule 7.65 s; gen_v1/REPORT.md:41 'the model buys its margin by driving slower'. Twin data: P(fail CRM | ok rigid) = 0.60, so a CRM-biased model picks slower rigid routes that still reach the goal. PLAN.md:35-37 predeclares only goal-reached for A.
- Consequence: A pooled model, or H with an all-masked history (what A3 evaluates), passes the rigid test regardless of whether it adapts; 'preserves specialist performance on each domain' is vacuous on one domain and the cross-specialist control is uninterpretable there.

**9. [major] A5 established-history closed loop and A4 retraining (PLAN.md:43-49, 87-88, 90-95, 158-159)**

- Evidence: PLAN.md:46 fixes S_crm = CRM_N2_s0-4, S_rigid = N2_s0-4 (deployed models fitted on station_ds startup rows, nctx 5; risk_model_dataset.md section 1: twin state at rest); PLAN.md:88 'Hist arms are retrained on reanchor + branch rows' (only hist arms); PLAN.md:24,55: startup rows are the only masked rows; PLAN.md:93 masks the history at frame 60 at ~3 m/s; PLAN.md:159 uses T as the ceiling. The uncontaminated same-data reference exists only as the --domain-filter arm (PLAN.md:67) and is not driven.
- Consequence: If H beats S at frame 60 the gain cannot be attributed to history rather than to moving-anchor training data; if S beats H the plan reads it as history failing. The masked ablation may behave erratically and exaggerate the history benefit. The A5 read-out is uninterpretable as written.

**10. [major] A5 common approach (PLAN.md:90-92)**

- Evidence: Recomputed this session from grid.npz along a 12 m straight at start_yaw for the 200 f104_crm_eval starts: 80/200 have max |grade| > 12 deg, 53/200 > 17 deg, 31/200 mean uphill > 8 deg. crm_f104_v1/LOG.md:6: CRM stalls from rest at full throttle on 12-17 deg slopes. scripts/crm_collect.py:199-200 and traverse_fdm_rgbd_diverse_chrono.py:193-194 raise unless the route ends within 0.25 m of the goal; gen_planner.py:94-99 safe_validate checks curvature/extent only.
- Consequence: In pass 1 up to ~25-40 % of CRM groups reach frame 60 stalled or digging; pass 2 compares arms from a state none can rescue, the established read-out is dominated by the approach, and the 11 node-hours buy fewer discriminating groups than the startup protocol.

**11. [major] A4 anchor sampling and offline established read-out (PLAN.md:56-57, 84-85, 97)**

- Evidence: Verified: reanchor_crm.npz split groups train 1,089 / val 56 / test 55; held-out established rows 3,876 with fail 0.717 vs startup 0.730 (the biased set, crm_night2_v1/REPORT.md section 2). The blacklist at PLAN.md:56 covers suite ids only, not held-out group ids.
- Consequence: Either the offline held-out AUC/calibration for established history is contaminated, or it is computed only on data the plan itself calls biased; either way the offline established read-out is uninterpretable.

**12. [major] Held-out set reuse for selection (PLAN.md:56-57, 111-112, 128-134, 160)**

- Evidence: PLAN.md:57,112 held-out = 111 twin groups (val + test); :133 gate on held-out stalled windows; :160 'gate fails after two model variants'; scripts/n2_arch_train.py:202 pools split != train as held-out; traverse_wp2_train_map.py:719-733 selects ckpt_best on the val split.
- Consequence: Offline held-out numbers are optimistically biased by selection; the milestone read-outs are not on a sealed set.

**13. [major] A4 'CRM prefixes reproduce bit-identically' (PLAN.md:82,84), own-remainder continuation (PLAN.md:86), stop rule (PLAN.md:161)**

- Evidence: Verified on the cluster this session: the recorded collector_sha256 26634a90 equals C/source/scripts/crm_collect.py; the local scripts/crm_collect.py is cb6792be (a different file). The branch mode must fork run() because the swap point (crm_collect.py:225-306) has no hook receiving driver/xy/speed/wp (collector_timing.md section 4). Cluster determinism was shown on the HIP build across GPU types (LOG.md, REPORT.md:48) with mbs_threads 4 / CRM_OMP 4; the local build is CUDA and the local smoke dirs hold only wall_s, so run-to-run determinism on the 5090 is unmeasured.
- Consequence: If the fork or thread count perturbs the loop, every anchor's 'recorded pose at F' differs from the branch pose and the measured history no longer matches the branch label; the byte-for-byte stop rule at line 161 passes vacuously or fails spuriously on CUDA atomics rather than on the code change. If bit-identity holds, one third of the 2,400 CRM branch drives (~4 node-hours) duplicate reanchor rows.

**14. [major] A4 rigid branches (PLAN.md:84-87: 'all branches of an anchor on one node', continuations 'from the recorded pose at F', 320 low-progress anchors)**

- Evidence: memory/amd-not-reproducible-across-nodes.md: same code on another node diverges at frame 2, max |delta action| 0.28, marginal stall/timeout outcomes flip (~5 % of marginal runs); production_v3/v4 ran on arbitrary nodes. The plan selects the 320 anchors by a stall seen in the recording and plans continuations from the recorded pose.
- Consequence: An unknown fraction of the rigid low-progress anchors are not stalled at F on the replay node and branch routes start off the true pose; branch_rigid labels attach a moving history to a route family sampled for a different state.

**15. [major] A4 low-progress anchors 'cut 1 s after onset' (PLAN.md:85-86)**

- Evidence: Recomputed this session on 669 local collect_v1 breakthrough episodes (onset = first 20 consecutive frames with |vx| < 0.3 and throttle > 0.3): onset p50 10.1 s, onset-to-termination p10/50/90 = 3.9/6.8/12.1 s, mean-spindle sinkage increase between onset-1 s and onset+1 s p50/p90 = 0.11/0.19 m (breakthrough fires at sinkage > 0.30 m for 5 frames, crm_collect.py:287-290). The branch driver starts with throttle 0 and no reverse/recovery command exists (gen_collect.py:76).
- Consequence: A large share of continuations from these anchors end in soil_breakthrough regardless of route or speed; the 320x3 branch rows carry near-constant labels and the handoff's 'feasible recovery / low-progress' data source is spent on ~12 node-hours of foregone conclusions.

**16. [major] A4 branch labels (PLAN.md:87 'Labels by the night-2 labeller on the branch segment')**

- Evidence: scripts/f104_n2_analyze.py:16-24 computes back_s/min_vx over vx[S0:] of the full trajectory and fail from outcome.status; scripts/n2_reanchor_dataset.py:36-39: ns = first_run(|vx|<0.3 & throttle>0.3, 20 frames) fires at the first frame after the skip if still stalled, ev = min(rb, ns), :70 maps it to eidx ~ 0, and any rollback makes the row unsafe regardless of goal outcome; outcome.json sustained_near_stop / low_net_progress_4s_windows / bounded_blockage_v1 are computed over prefix + branch (crm_collect.py:322-333, 359-368).
- Consequence: Low-progress branch rows are all 'unsafe' with event at station 0 by construction and clean-moving rows inherit prefix rollback; the branch npz labels do not measure the continuation and the 'history matters where bogging happened' claim is untestable from them.

**17. [major] B0 (PLAN.md:102-104), B3 stall rule (PLAN.md:123-125), B5 'PID arm in the same job' (PLAN.md:140), thresholds (PLAN.md:36-39)**

- Evidence: Unmodified collectors stop a stalled vehicle only after 24+2+8 s with throttle > 0.3 in all 40 frames (gen_collect.py:126-153); PLAN.md:124-125 adds a 4 s rule at vx < 0.3 with throttle > 0.05 for policy drives only. A PID driven through gc_collect_ext.py holds one action per 50 ms, whereas the recorded PID refreshes every substep (crm_collect.py:234-246); the handoff milestone is 'beats the current PID'.
- Consequence: A stalled policy run ends ~30 s earlier than a stalled PID run, so the completion rule and the failure-inclusive cross-track compare different horizons; a held-PID reference is not the deployed PID, so the milestone claim is unverifiable either way.

**18. [major] Conventions (PLAN.md:27-28 'hold one action for the whole 50 ms interval and record exactly that action'), B3 (PLAN.md:121-123 '2/s steering rate')**

- Evidence: Both loops clamp steering every substep to previous_steer +/- 2*dt and capture the row at sub 0 from the clamped inputs (crm_collect.py:236-246, traverse_fdm_rgbd_diverse_chrono.py:224-230); with dt = 1 ms and a held command, steering moves 0.002 per substep toward cmd and the row records previous_steer +/- 0.002. The precedent that gets it right clips once per 20 Hz step and writes the same inputs at every substep (traverse_wp3_chrono_eval.py:139-140, 469-470).
- Consequence: B4 trains the NRD on steering transitions whose logged value lags the applied mean by up to 0.05-0.1 exactly at turn initiation, reintroducing the mismatch B2 was meant to remove; hold_ok filtering cannot detect it.

**19. [major] B3 'policy: torch actor on CPU' (PLAN.md:122), B5 CRM evaluation via gc_collect_ext.py --mode policy (PLAN.md:140)**

- Evidence: Verified on the cluster: /work1/dannegrut/harry/nrd/env.sh:38,41-43 puts the pytorch/2.10.0 module tree on PYTHONPATH for the collector; memory/amd-hpcfund-cluster.md: that torch crashes on MI210 compute nodes ('HIP error: file not found' on first matmul); the FSI pychrono links ROCm 7.2 by RPATH (cluster_data_inventory.md section 4), so torch + pychrono FSI would load two ROCm runtimes in one process; 33 of the 47 crm_launch.sh tasks are MI210 partitions (crm_launch.sh:15-17).
- Consequence: The B5 CRM policy evaluation and the failure-harvesting round either fail at import on most nodes or run with a corrupted HIP context; the CRM half of Milestone B is unevaluable.

**20. [major] Order and budget (PLAN.md:146-151 'Wave 1 (parallel)', 'Wave 2')**

- Evidence: scripts/crm_launch.sh:15-21 submits 24+6+3+2+1+5+6 = 47 tasks; n2_rigid_eval_tasks.py defaults to 24 shards; n2_train.sbatch is an array of 7; memory/crm-f104-night-state.md:46 'max 50 queued jobs per user (array tasks count)'. Wave 1 = 47 + 24 = 71; Wave 2 (A3 + B3 + A4 CRM launches) = 141. cluster_data_inventory.md notes the cap is a memory note not re-verified today.
- Consequence: Later submissions are rejected or the operator serialises waves by hand, breaking the parallel schedule; partial launches on a subset of partitions change the cost mix.

**21. [major] B5 fragments and observation history (PLAN.md:137-139) vs Chrono evaluation from rest**

- Evidence: src/nedm/traverse/tracker_env.py:309-313: fragment start frames are drawn from [context(=16), active_end - len]; the 8-past-action/8-past-state block in Chrono would be padded by repeating frame 0 (traverse_wp3_chrono_eval.py:427-431 pads with hist[0]), never seen in training; NRD windows do start at t0 = 0 (traverse_wp2_train_map.py:148-155) but with a real context.
- Consequence: Launch-from-rest on slopes (rollback, wheel spin in CRM) is the phase the PID handles by design; a policy that has never seen its own padded startup can stall or roll back in the first 1-2 s and lose the paired comparison for a reason unrelated to tracking.

**22. [major] B4 gate and hold_ok targets (PLAN.md:128-134) with the drift filter (PLAN.md:27-29)**

- Evidence: The PID's exclusive throttle/brake switch (chrono_vehicle/driver/ChPathFollowerDriver.cpp:97-108, verified) makes every brake onset a flip by construction, which the plan's rule ('no throttle/brake flip') removes. ML reviewer's measurement on 600 episodes per set: CRM removes 100 % of 850 brake onsets, rigid v3 100 % of 2,055, v4 100 % of 4,361, vs 0.8-1.5 % of stalled transitions.
- Consequence: The mixed NRD is fitted and validated on a set biased toward steady-state and stalled frames; brake response is learned only from B3's perturbed taps (p 0.05), and the gate can pass while imagined brake dynamics are wrong (WP3 arena_v1 evidence covers no stalls or slopes).

**23. [major] B1 cache schema (PLAN.md:108-112) and B4 training on mi3501x (PLAN.md:133-134)**

- Evidence: src/nedm/traverse/nrd_data.py:116-122 pads every episode to lengths.max() (verified; max 1,487 frames CRM, 2,400 rigid vs means 422-944); MapBatcher casts arrays to float32 (traverse_wp2_train_map.py:59-63); 24,000 x 2,400 x 256 x 4 B = 59 GB for rigid terrain16 alone on a 62 GB machine. rollout_loss (train_map.py:227-228), rollout_eval (:319-322) and the tracker env (tracker_env.py:357) call model.cropper at the dead-reckoned pose.
- Consequence: gb_build_cache.py / gb_train_nrd.py OOM locally or spill to swap; on the cluster the 4 h cap is consumed by loading; the terrain8-then-terrain16 switch means rebuilding the cache twice.

**24. [major] B5 PID-imitation warm start (PLAN.md:139) with rsl_rl empirical observation normalisation**

- Evidence: Verified: scripts/traverse_wp3_train_tracker.py:145 empirical_normalization True; rsl_rl/runners/on_policy_runner.py:68-70 EmpiricalNormalization(until=1.0e8), :157 normalises obs before the actor; rsl_rl/modules/normalizer.py:52 updates statistics whenever training; 2048 envs x 64 steps = 131k samples per iteration.
- Consequence: The warm start either does nothing (actor trained on raw obs, then fed normalised obs) or drifts with the normaliser; the plan would report 'warm start did not help' from an implementation artefact.

**25. [major] A2 calibration metrics (PLAN.md:69) and Milestone A read-out (PLAN.md:97-98)**

- Evidence: Verified: twin_rigid.npz fail 0.194 vs unsafe 0.361, 2,517 rows unsafe-and-not-fail, 0 fail-and-not-unsafe, 0 rows with event_idx == -1 and unsafe == 1; held-out rigid fail 0.186 vs unsafe 0.359; twin_crm fail == unsafe. scripts/n2_arch_train.py:228-233 trains on event_idx.
- Consequence: A per-domain calibration table would show the rigid model 'over-predicting' by ~0.17 while CRM looks calibrated, and a reader would attribute a label artefact to the shared model or to domain adaptation.

**26. [minor] A0 suite composition (PLAN.md:43-49)**

- Evidence: eval_iter_crm/analysis.txt: B 98.5 %, C 98.5 %, E 95.5 %, F 94.5 % on the same 200 groups; CRM is bit-identical so re-driving cannot remove the selection (crm_f104_v1/REPORT.md:48).
- Consequence: The non-inferiority margin is consumed by a fraction of a point of reference inflation on one third of the suite.

**27. [minor] Milestone A read-out on suite drives (PLAN.md:97-98)**

- Evidence: f104_n2_iter.py:312-315 pick = argmin objective over evaluated candidates; night-2 suite mean predicted P of picks 0.006-0.05 (analysis.txt).
- Consequence: AUC over picks reflects disagreement between planners rather than ranking quality; calibration is assessed only at P < 0.05 and looks good by construction.

**28. [minor] A1b identifiability probe (PLAN.md:59-62)**

- Evidence: risk_model_dataset.md section 4: 57,444 CRM vs 58,424 rigid re-anchored rows, only 44,990 (episode, frame) pairs coincide; n2_reanchor_dataset.py:45-49 stratifies anchors over speed within each world. PLAN.md:24 defines startup windows as all-masked; the critic's 0.55-0.60 (critic.md:65-67) was measured on the 17-D rest state including tyre forces, which the deployable 12 columns exclude.
- Consequence: The probe could report AUC > 0.7 and clear the stop rule (PLAN.md:158) without the history identifying the soil, and the informative startup question (does the single settled frame on the 12 deployable columns carry domain information) is not asked.

**29. [minor] A2 hist_rma arm (PLAN.md:66-67) vs handoff step 2 (PLAN_handoff.md:26)**

- Evidence: scripts/n2_arch_train.py:60,72: ctx embedding = GELU(Linear(ctx)), with tag appended to the 5-d geometry ctx (PLAN.md:64); the history encoder sees only hist. hist_aux already fits a BCE head on z (PLAN.md:66). critic.md:65-67: the rest state carries <= 0.60 AUC, so no other privileged continuous signal enters the teacher.
- Consequence: A wasted arm and a report citing RMA as precedent for something equivalent to domain classification. Downgraded to minor: it wastes compute but does not corrupt the other arms.

**30. [minor] A1 mixed dataset construction (PLAN.md:51-55)**

- Evidence: Verified this session: reanchor_crm.npz has 15,024 '@0' rows, all 15,024 episode ids in twin_crm ids; X max |diff| 0.0, fail and event_idx equal on the checked row. So the mixed set holds 30,048 duplicated rows (~20 %) and the 1,395 held-out startup rows per world appear twice.
- Consequence: Startup rows get double weight in the survival loss and are double-counted in the per-domain startup metrics, Brier and ECE; ids differ only by the '@0' suffix so no assertion catches it.

**31. [minor] Budget figures (PLAN.md:49, 87, 94, 126, 152-153) and B4 on mi3501x (PLAN.md:133-134)**

- Evidence: Sim reviewer's rate table from scontrol TRESBillingWeights (mi2101x 0.1, mi3501x 0.125, mi2104x 0.4, mi2508x 0.8, mi3008x 1.0, mi3508x 1.2 billed per raw node-hour; reproduces the ledger's 489.9 billed over 1288 raw hours and the CRM night's ~37). 'B4 12 node-hours on mi3501x' is 96 wall-hours at 0.125/h, i.e. 24 separate 4 h jobs; 2,400 CRM branch drives are ~37 GPU-hours, more than 7 idle mi3501x finish in one 4 h window. The 590 stop rule itself is safe (the CRM figures are conservative).
- Consequence: CRM steps need multiple resubmissions or the 8-GPU partitions (39 jobs pending on mi3008x today), and the B4 figure cannot be checked against the cap as written.

**32. [minor] B4 'resume from checkpoint if needed' (PLAN.md:133-134); B5 PPO under the same cap**

- Evidence: Verified: scripts/traverse_wp2_train_map.py:600,638 --init-from loads the model state with strict=False; AdamW is created fresh at :658 and the step counter/schedule restart. scripts/traverse_wp3_train_tracker.py:140 sets 'resume': False, 'resume_path': None.
- Consequence: A 4 h-capped B4/B5 run that is cut off restarts its optimiser and LR schedule, so reported epochs and any early-stopping gate refer to a different optimisation than intended.

**33. [minor] A4 driver swap (PLAN.md:83 'make_driver(branch), Reset(), no Initialize(), steering state kept')**

- Evidence: Verified: scripts/traverse_fdm_rgbd_diverse_chrono.py:111 driver.Initialize() inside make_driver; chrono_vehicle/driver/ChPathFollowerDriver.cpp:131-141 the constructor calls Reset(); ChClosedLoopDriver::Initialize (:59-63) adds a fixed ChBody to the system mid-run.
- Consequence: Either a body is added to the live system at frame F (untested under CRM coupling with mbs_threads 4) or the code diverges from the plan text.

**34. [minor] Wave 1 local work (PLAN.md:146-147) and B2 (PLAN.md:114-118)**

- Evidence: crm_f104_v1/LOG.md: 4 M particles, 0.25x real time on the 5090 at 0.5 ms, four processes per GPU 9x slower each; smoke_report.json records only wall_s. The repo root has no source_manifest.json and nav_local_batch.py references no FDM_RUNTIME_FINGERPRINT, so a local rigid run must bypass gen_collect.main() (as rigid_moving_collect.py:22-34 does).
- Consequence: Running A2 concurrently with the 10-episode CRM audit and the collector smokes can turn a 15-minute audit into hours or OOM the CRM process; a passing local rigid smoke says nothing about the gates the cluster run will hit.

**35. [minor] B1 cache fields and B4 gate on 'held-out stalled windows' (PLAN.md:110, 132-133)**

- Evidence: PLAN.md:110 lists status but no per-frame stall flag; frame-level data exists in outcome.json bounded_blockage_v1_windows / low_net_progress_4s_windows (crm_collect.py:322-332) or directly from trajectory.npz (|vx| < 0.3 & throttle > 0.3); stall runs >= 60 frames: 414 in 600 CRM episodes, 110 rigid v3, 447 v4 (ML reviewer's count).
- Consequence: gb_train_nrd.py would re-open raw run dirs at validation time or the gate would be improvised per run.

**36. [minor] A3 startup closed-loop arms (PLAN.md:76-78)**

- Evidence: PLAN.md:24 (startup rows all-masked) and :77 (closed loop at startup); A2's metrics per domain x startup (PLAN.md:69) already compare H, P and T offline on the same rows. (H and P are not identical models, since H's weights are shaped by established rows, so this is a budget point, not an equivalence.)
- Consequence: ~3-4 node-hours spent on a near-duplicate arm within a 100 node-hour cap.

**37. [minor] B5 choice of frozen NRD (PLAN.md:129, 136)**

- Evidence: PLAN.md:129 (arms tag, notag), :136-139 (PPO in the frozen NRD, domain tag only as a diagnostic arm for the policy).
- Consequence: An env-privileged tag NRD with a tag-blind policy is a legitimate but unstated design; results across arms become incomparable if the NRD differs.

**38. [minor] A2 compute estimate (PLAN.md:70)**

- Evidence: stageC/crm_gru_geom_lr0.002_holdout.json: 99-120 s/seed at 41k fit rows; mixed fit rows ~105k plus GRU(15->32) over 40 steps; arms none/tag/hist/hist_aux/hist_rma x holdout+deploy x 5 seeds plus two --domain-filter references ~70 runs.
- Consequence: Wave-1 scheduling assumes A2 finishes alongside A0 drives; a 6-8 h local job blocks A3 pick generation.

