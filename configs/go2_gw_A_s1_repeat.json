{
  "processed_dataset_dir": "/home/kyle/sbel-artifacts/training_datasets/go2_gravworld_A",
  "output_dir": "/home/kyle/sbel-artifacts/training_runs/go2_gw_A_s1_repeat",
  "validation_dataset_name": "flat",
  "terrain_conditioning": {
    "enabled": false,
    "terrains": [
      "flat"
    ]
  },
  "train_mix": {
    "datasets": [
      {
        "name": "walking",
        "terrain": "flat",
        "processed_dataset_dir": "/home/kyle/sbel-artifacts/training_datasets/go2_gravworld_A",
        "batch_fraction": 1.0
      }
    ]
  },
  "validation_datasets": [
    {
      "name": "flat",
      "terrain": "flat",
      "processed_dataset_dir": "/home/kyle/sbel-artifacts/training_datasets/go2_gravworld_A",
      "split": "val",
      "max_val_batches": 200
    }
  ],
  "validation_loss_weights": {
    "flat": 1.0
  },
  "loss": {
    "type": "huber",
    "huber_delta": 1.0,
    "channel_weight_mode": "equal_domain_combined_std",
    "channel_weight_datasets": [
      "/home/kyle/sbel-artifacts/training_datasets/go2_gravworld_A"
    ]
  },
  "rollout_eval": {
    "num_episodes": 12,
    "horizons_s": [
      5.0,
      10.0
    ],
    "selection_horizon_s": 10.0,
    "datasets": [
      {
        "name": "flat",
        "terrain": "flat",
        "processed_dataset_dir": "/home/kyle/sbel-artifacts/training_datasets/go2_gravworld_A",
        "split": "val",
        "weight": 1.0
      }
    ]
  },
  "model": {
    "block_size": 128,
    "n_layer": 6,
    "n_head": 8,
    "n_embd": 256,
    "dropout": 0.0,
    "bias": false,
    "head_hidden_dim": 256
  },
  "optimizer": {
    "lr": 0.0003,
    "min_lr": 3e-05,
    "weight_decay": 0.1,
    "betas": [
      0.9,
      0.95
    ],
    "warmup_steps": 1000,
    "grad_clip_norm": 1.0
  },
  "training": {
    "seed": 2026090701,
    "device": "cuda",
    "batch_size": 64,
    "num_workers": 0,
    "num_epochs": 80,
    "steps_per_epoch": 2000,
    "max_val_batches": 200,
    "checkpoint_metric": "rollout_sel",
    "load_dataset_into_memory": true,
    "pin_memory": false,
    "compile": false,
    "input_noise_sigma": 0.0
  },
  "sweep_recipe": {
    "version": "go2_gw_A_s1_repeat",
    "slug": "go2_gw_A_s1_repeat",
    "notes": "SAME-BOX REPEAT of go2_gw_A_s1. Identical config, identical seed (2026090701), identical dataset, same machine. Nothing in this repo sets cudnn.deterministic, use_deterministic_algorithms or cudnn.benchmark -- verified by grep across src/nedm and scripts/training -- so a fixed seed pins data order and initialisation and nothing about kernel selection. This measures RUN-TO-RUN variance, which has never been separated from seed variance on this surrogate. It also makes any cross-machine number interpretable: cross-box difference minus this is the machine effect. Retrospective stake: the input_noise_sigma 2x2 reported seed effects of 5 and 11 surviving pairs and attributed them to the seed."
  }
}